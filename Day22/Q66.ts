//creating a function that takes two boolean arguments and returns a boolean value using && operator

let verification = function (con1: boolean, con2: boolean): boolean {
    return con1 && con2;
}

console.log(verification(false,true));

