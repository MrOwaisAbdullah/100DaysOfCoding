//creating a function that combine a text and a number and make them string

let combineString = function (string: string, number: number): string {
    return string + number;
};
console.log(combineString("Age: ", 24));