//writing a function that takes a decimal number and returns the number rounded to the nearest integer.

function roundNum(num: number){
    return Math.round(num);
}

console.log(roundNum(2.3));
console.log(roundNum(2.6));