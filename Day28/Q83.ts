//creating a function that convert the case of string in uppercase and lowercase.
function upperLowerCase(value: string){
    let upperCase = value.toUpperCase();
    let lowerCase = value.toLowerCase();
    console.log(`UpperCase: ${upperCase}\nLowerCase: ${lowerCase}`)
}

upperLowerCase("Owais Abdullah");