//creating a function that assigns a variable and then updated the variable inside the function
function variables() {
    let variable = "Owais";
    console.log(`Initial Value: ${variable}`);

    variable = "Ali"
    console.log(`Updated Value: ${variable}`);
}

variables();