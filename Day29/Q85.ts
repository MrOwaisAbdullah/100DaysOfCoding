//creating a function that tell about the index of word "code" in a string

function findPosition(str: string):number {
    return str.indexOf("code");
}

console.log(findPosition("I write code in TypeScript"));
